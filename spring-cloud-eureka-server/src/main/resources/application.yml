server:
  port: 8100
spring:
  application:
    name: eureka
eureka:
  instance:
    hostname: 127.0.0.1
  client:
    service-url:
      defaultZone: http://localhost:9100/eureka,http://locahost:9200/eureka
    register-with-eureka: true
    fetch-registry: true
  server:
    enable-self-preservation: true
    eviction-interval-timer-in-ms: 2000
    